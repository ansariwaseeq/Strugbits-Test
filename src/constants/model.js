"use strict";

const MODEL = {
  USER: "User",
  CHAT:"Chat"
};

const COLLECTION = {
  USER: "users",
  CHAT: "chats"
};

const TIMESTAMPS = {
  createdAt: "created_at",
  updatedAt: "updated_at",
};

module.exports = {
  MODEL,
  COLLECTION,
  TIMESTAMPS,
};
